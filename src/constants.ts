export enum STORES {
  AUTH_STORE = 'authStore',
  COUNTER_STORE = 'counterStore'
}

export enum PAGE_PATHS {
  SIGNUP = '/signup',
  SIGNIN = '/signin',
  COUNTER = '/counter',
  PRIVATE = '/private'
}
